#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);
	
	int teams[100];
	int total[100];
	int seat[100][100];
	
	for (int i = 0; i < n; i++){
		scanf("%d", &teams[i]);
		total[i] = teams[i] * 8;
	}

    
    //第一阶段
    for(j)
    for(i)
    seat[i][j] = seat_num++;
    //每坐下一个
	total[i]--;
	//有学校完蛋了
   if (total[i] == 0)
	n--;
	
	//第二阶段，直到n == 1，那个学校隔一个做一个